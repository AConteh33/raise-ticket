# Issue Tracker

HTML/CSS/JS frontend in `html-site/`. Express server (`html-site/server.js`) serves static files and API routes. SQLite database in `db/`.

Run: `npm run dev` (port 8080). Seed admin: `npm run db:seed`.
